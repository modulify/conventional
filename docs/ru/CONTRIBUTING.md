# Contributing

[🌐 Переводы](../../docs/INDEX.md#contributing) • [📜 Кодекс поведения](../../CODE_OF_CONDUCT.md)

Спасибо за вклад в `modulify/conventional`.

Этот документ описывает наши ожидания к качеству вклада.
В этом репозитории мы поддерживаем высокий инженерный стандарт и ожидаем
такой же дисциплины от каждого изменения.

## Контекст репозитория

`modulify/conventional` - это Yarn workspaces monorepo:

- `packages/conventional-git`
- `packages/conventional-bump`
- `packages/conventional-changelog`

Границы workspace считаются важным архитектурным ограничением.
Изменения должны оставаться четко ограниченными по области и логически разделенными.

## Локальная подготовка

Перед началом работы мы используем:

```bash
make .yarnrc.yml
yarn install
```

## Ожидания по качеству

Перед передачей работы или PR мы ожидаем успешное прохождение:

- Тестов:
```bash
make test
```
- Покрытия:
```bash
make test-coverage
```
- Проверки типов:
```bash
make typecheck
```
- Eslint, если изменены файлы, попадающие под линтинг (`*.ts`, `*.js` и т.д.):
```bash
make eslint
```

Пороги покрытия в конфигурации строгие:
- `100%` statements
- `100%` branches
- `100%` functions
- `100%` lines

Внешние вклады, ухудшающие качество покрытия, не принимаются.

## Definition of Done

Вклад считается завершенным, когда выполнены все пункты ниже:

- Изменение четко ограничено нужным workspace и поведением.
- Обязательные проверки пройдены (`test`, `test-coverage`, `typecheck` и `eslint`, когда применимо).
- Документация обновлена, если изменились поведение или публичный API.
- Коммиты разделены по логическому смыслу и границам workspace.

## Философия покрытия

Покрытие в этом репозитории - это порог качества, а не метрика тщеславия.

Для внешних вкладов мы ожидаем такой порядок работы:

1. Сначала покрывать реальные сценарии публичного API.
2. Добавлять пропущенные реалистичные сценарии.
3. Рассматривать оставшийся непокрытый код как сигнал качества:
   - потенциальный источник багов,
   - избыточная/мертвая логика,
   - архитектурный запах.
4. Добавлять контролируемые сценарии отказа для защитных веток.
5. Предпочитать удаление избыточной логики или улучшение архитектуры вместо искусственных тестов для невозможных путей.
6. Если код уже покрыт реальными сценариями применения публичного API (и штатными, и аварийными), но общее покрытие все равно ниже `100%`, это считается признаком неудачного дизайна и/или бага, а не основанием добавлять синтетические edge-case тесты ради метрики.

Эта философия зафиксирована в:
- `skills/coverage-recovery/SKILL.md`

## Практический совет для AI-парной разработки

Работая с AI-агентами в этом репозитории, не заставляйте своих агентов ходить кругами.

Если после разумного числа попыток прогресс остановился, запрашивайте эскалацию вместо очередного brute-force подхода:

1. Запросите точный отчет о непокрытых путях и причинах.
2. Попросите конкретные варианты и tradeoffs:
   - оставить защитный код и принять разрыв,
   - переработать архитектуру для тестируемости,
   - удалить избыточные/невозможные ветки.
3. Явно зафиксируйте архитектурное решение до продолжения работы.

Финальные решения по спорным архитектурным компромиссам остаются за разработчиком-человеком.

## Flaky Tests

Flaky-тесты не принимаются во внешних вкладах.

- Не отправляйте изменения с заведомо flaky-поведением.
- Не помечайте нестабильное поведение как допустимое только потому, что проходит coverage.
- Сначала стабилизируйте сценарий, затем ожидайте review/merge.

## Чеклист PR

Перед открытием или обновлением PR проверьте:

- [ ] Scope выбран корректно и границы workspace соблюдены.
- [ ] Поведение публичного API покрыто тестами.
- [ ] `make typecheck` проходит.
- [ ] `make test-coverage` проходит.
- [ ] `make eslint` проходит, если изменялись файлы, попадающие под линтинг.
- [ ] Связанная документация обновлена.
- [ ] Сообщения коммитов соответствуют правилам репозитория.

## Правила коммитов

Мы ожидаем, что все коммиты соответствуют правилам репозитория:

- Формат Conventional Commits.
- По умолчанию сообщения коммитов на английском языке.
- Формулировка как завершенный исторический факт (кратко, удобно для changelog).
- Scope workspace для локальных изменений:
  - `conventional-git`
  - `conventional-bump`
  - `conventional-changelog`
- Для глобальных/manual-коммитов синтетический scope не используется (обычно scope опускается).
- Логическое разделение по workspace и намерению.
- `yarn.lock` в отдельном коммите с точным заголовком:
  - `chore: Updated yarn.lock`

Подробный commit flow:
- `skills/commit-workflow/SKILL.md`
