# Revert Cross-Workspaces Fixture (Planned)

Reserved for mixed revert scenarios where commits from different workspace
lines are reverted and reverted again.

Planned focus:

- ignore-reverted behavior in interleaved histories,
- revert matching by hash/header across tag boundaries,
- release recommendation stability in noisy real-world timelines.
