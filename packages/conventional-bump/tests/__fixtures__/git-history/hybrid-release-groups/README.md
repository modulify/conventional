# Hybrid Release Groups Fixture (Planned)

Reserved for a scenario where some workspaces move in sync and others move
independently.

Planned focus:

- group-level tag lines (`core@x.y.z`) mixed with workspace-level lines,
- range selection priority for `fromTag` vs `tagPrefix`,
- deterministic behavior when commit history is interleaved.
